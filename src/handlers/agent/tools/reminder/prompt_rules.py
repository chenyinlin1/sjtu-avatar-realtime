"""Shared LLM-facing rules for reminder tool usage."""

REMINDER_SYSTEM_RULES = """
强制规则：当用户明确要求创建普通提醒，且事项和具体时间完整时，
本轮必须只调用 manage_reminder，不得先输出任何自然语言；
不得用“记好了”“会提醒您”等话术代替工具调用。
时间含糊或缺失时必须先追问，不得猜测时间。
工具返回真实成功回执前，只能说正在处理，不能声称提醒已经创建成功。
工具失败或结果不明确时必须如实说明。

用户要求取消提醒时，必须先从当前提醒快照确定唯一 entity_id；
无法唯一定位时先追问，不得猜测。
取消是破坏性操作，首次提出取消时只复述具体提醒并询问是否确认，不执行取消。
只有用户在后续一轮明确确认后，才能调用取消工具并传 confirmed=true。
普通提醒工具不得修改正式用药方案，也不得关闭危急预警、ALERT、SOS 或医疗安全工单。
取消同样必须等待真实成功回执，成功回执前不得声称已经取消。
""".strip()


REMINDER_TOOL_DESCRIPTION = (
    "创建或取消普通关怀提醒。用户明确要求创建提醒，且事项和具体时间完整时，"
    "必须调用 operation=create；本轮不得先输出自然语言，也不得口头假装已经创建。"
    "时间含糊或缺失时必须先追问。remind_at 可传结合系统当前时间算出的 ISO 时间、"
    "epoch 毫秒或完整自然语言时间。operation=cancel 时，必须从当前提醒快照确定唯一 "
    "entity_id，先复述具体提醒并取得明确确认；只有确认后的下一轮才传 confirmed=true。"
    "不得按标题模糊取消或猜测 entity_id。只可操作普通关怀提醒；不得变更正式用药方案，"
    "不得关闭危急体征预警、ALERT、SOS 或医疗安全工单。必须等待真实 action_ack，"
    "在 ok=true 前不得告诉用户已经成功。"
)


REMINDER_TOOL_MISSING_REPLY = "这个提醒还没记上，请您再说一遍。"
